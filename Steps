Steps:

-Need to clear input bar
	Resets variable and therefore doesn't make proper request
-Upon re-search, userSearch is replaced with new input value b/c the value is tied to the scope.

If there was an error calling to the server, then there should be a message indicating this to the user.	
